# SSOP containment — firewall block/unblock + read (network host only)
# Narrow whitelist: only the exact block/unblock/verify patterns, no arbitrary iptables.
rdrolfe ALL=(ALL) NOPASSWD: /usr/sbin/iptables -I INPUT -s *, /usr/sbin/iptables -D INPUT -s *, /usr/sbin/iptables -L INPUT -n
