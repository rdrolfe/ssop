# Secrets — never commit
.env
deploy/.env
*.pem
*.key
agent-ssh
hermes_ssop

# Runtime artifacts
audit/
tickets/
__pycache__/
*.pyc
.venv/
agent-env/

# Local
*.log
.DS_Store
ssop-share*

# Isolated worktrees (see branch-workspace-hygiene skill) — must stay ignored
# so a worktree's contents can never be swept into this repo
.worktrees/
worktrees/
